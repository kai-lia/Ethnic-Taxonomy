# Ethnic-Taxonomy

Install requirements: note this is very large so at your own risk
- run everything via utils.main.py
  - execute all analysis run "python -m utils.main" make sure to be in the directory root
  - there is a y/n if you want embeddings (will take awhile)

